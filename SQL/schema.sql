CREATE TABLE investments (
    client_id VARCHAR,
    age INT,
    city VARCHAR,
    asset_type VARCHAR,
    risk_level VARCHAR,
    provider VARCHAR,
    total_investment NUMERIC,
    current_value NUMERIC,
    returns NUMERIC,
    date DATE,
    currency_type VARCHAR
);
